# MVP・Phase 5・UX監査受入確認ハンドオフ

> **履歴資料の注意（2026-08-19）**: 本書は2026-07-18を中心とするPhase 5受入・UX監査の記録であり、現在状態のSource of Truthではありません。現在の実装状況は `docs/current-state.md`、検証結果は `docs/testing.md`、継続中の問題は `docs/known-issues.md` を参照してください。本文中の「未実装」「受入済み」「12型のみ」などの日付時点の記述を、現在の状態としてそのまま採用しないでください。

更新日: 2026-07-18
対象: docs/要件定義書_v1.0.md AC-001〜AC-016、AC-101、AC-201〜AC-204
Phase 5実装コミット: 本Phaseの最終コミット

## AC確認状況

| ID | 状態 | 検証内容・残る確認 |
|---|---|---|
| AC-001 | テストで検証済み | 10,000mm校正後の再測定をphase1Acceptance.test.tsで確認。 |
| AC-002 | テストで検証済み | 910mm校正と450mm椅子の実寸比率を確認。 |
| AC-003 | テストで検証済み | ズーム・パンがmm正本を変更しないことをtransform.test.tsで確認。 |
| AC-004 | テストで検証済み | PDFページ、背景90度回転、切り抜きの保存・復元を確認。 |
| AC-005 | テストで検証済み | 椅子の500×550mm編集と外接矩形を確認。 |
| AC-006 | テストで検証済み | 回転後もwidthMm/depthMmを入れ替えないことを確認。 |
| AC-007 | テストで検証済み | 水平・垂直の整列と等間隔配置を確認。 |
| AC-008 | テストで検証済み | 配置・移動・回転・削除のUndo/Redoとドラッグ履歴を確認。 |
| AC-009 | テストで検証済み | 背景、校正、オブジェクト、heightMmのJSON保存復元と旧形式移行を確認。 |
| AC-010 | 実装済みだが人間の確認待ち | mm正本からPNGを再描画し編集UIを除外。ブラウザで背景・配置物の相対位置を目視確認する。 |
| AC-011 | 実装済みだが人間の確認待ち | pdf-libでA4/A3・縦横のPDFを生成。PDFビューアで1ページと情報欄を確認する。 |
| AC-012 | 実装済みだが人間の確認待ち | 1:50/1:100のPDF寸法計算をテスト済み。100%印刷し、1820mm基準線を実測する。 |
| AC-013 | テストで検証済み | 未校正時の配置・出力禁止、警告、ライブラリ無効化を確認。 |
| AC-014 | テストで検証済み | 非対応形式、破損PDF、破損JSONを安全に拒否することを確認。 |
| AC-015 | 実装済みだが人間の確認待ち | Pointer Events、ピンチ／パン、回転、レスポンシブUIを実装。実機iPad Safariで一連の操作を確認する。 |
| AC-016 | テストで検証済み | 山台のheightMm=300、段高ラベル、保存復元を確認。 |
| AC-101 | テストで検証済み | appState.phase4.test.tsで14プルト=椅子28脚＋譜面台14台を1 Actionで生成し、arrangement.test.tsで弧状座標と15度単位の向きを確認。 |
| AC-201 | テストで検証済み | scene3d.test.tsで壁線分・閉じた壁・全オブジェクト・アバター・背景の校正済みmm寸法を確認。ブラウザでも遅延読み込み後の3Dキャンバス表示を確認。実図面の壁・背景位置は人間確認待ち。 |
| AC-202 | テストで検証済み | scene3d.test.tsで椅子の座奏目線1200mm、回転方向、山台上の視点高さを確認。選択席→3D→「この席から見る」の導線をブラウザで確認。 |
| AC-203 | テストで検証済み | scene3d.test.tsで段高300mmを椅子アバターのbaseHeightMmと目線高さへ反映することを確認。 |
| AC-204 | 実装済みだが人間の確認待ち | 一人称視点の回転量テスト、Three.js canvasへ直接登録したPointer Events、canvas直下のtouch-action:none、Pointer Capture、ミニマップ移動を実装。実機iPad Safariで回転・移動・30fpsを確認する。 |

## UX監査の実施内容

- コード、reducer、既存テスト、保存／出力導線を確認。
- 開発サーバーをブラウザで操作し、背景読込済み・校正済み・オブジェクト配置後の状態を確認。
- 768×1024px（iPad縦持ち相当）でDOM、レイアウト寸法、コンソール警告／エラーを確認。
- 校正、単発配置、モード切替、矢印キー移動、出力ダイアログを実操作で確認。
- ブラウザ監査時のコンソール error/warn は0件。

## 発見事項と対応状況

| ID | 優先度 | 発見した問題 | 対応状況 |
|---|---|---|---|
| UX-AUDIT-001 | P1 | JSON「開く」が未保存変更を確認せず、現在の編集を破棄する可能性があった。 | 修正済み。未保存時は読み込み前に確認し、キャンセル時はファイル選択を開始しない。 |
| UX-AUDIT-002 | P1 | 通常の選択モードはツールバーの色だけで、現在モードの意味が常時伝わりにくかった。 | 修正済み。現在モード、配置待機、連続配置中をテキストとARIA状態で表示。 |
| UX-AUDIT-003 | P1 | 選択物を細かく動かすためのキーボード操作がなく、タッチ／マウスだけでは位置調整に手間がかかった。 | 修正済み。選択中の矢印キーを10mm、Shift+矢印を100mmのActionへ集約し、キャンバスをキーボードフォーカス可能にした。 |
| UX-AUDIT-004 | P1 | 768px幅では左右パネルとキャンバスが横に詰まり、タップ対象と作業領域が狭かった。 | 修正済み。900px以下はライブラリ→キャンバス→プロパティの縦配置に切り替え、主要ボタンと入力欄を44px以上にした。 |
| UX-AUDIT-005 | P2 | 校正距離の910/1820mmが数値だけで、半間・1間との対応を画面上で再確認しにくかった。 | 修正済み。ボタンを「半間・3尺（910mm）」「1間（1820mm）」と表示し、任意距離の説明も補強。 |
| UX-AUDIT-006 | P2 | プリセット寸法に単位がなく、ピアノやティンパニの値が正確な機種実寸なのか標準値なのか判断しにくかった。 | 実装済みだが人間の確認待ち。mm表示と「標準初期値・機種差あり」の説明を追加。実物仕様に合わせた最終値はホール備品／機種ごとに確認する。 |
| UX-AUDIT-007 | P2 | PDFの1:50、1:100、フィットの違いと印刷時の注意が1文だけで分かりにくかった。 | 修正済み。各縮尺の意味、1820mmの紙面寸法、フィット時の実寸非保証、倍率100%印刷を表示。 |
| UX-AUDIT-008 | P2 | プロパティの数値欄を空にしてから置き換える操作や、細かなタッチ領域には改善余地が残る。 | 将来対応。既存のAction・mm正本を維持したまま、入力コミット方式と実機操作を別途改善する。 |

## UX受入条件

| ID | 状態 | 根拠・残る確認 |
|---|---|---|
| UX-001 | テストで検証済み | ライブラリ、未校正バナー、校正ダイアログに背景読込→校正→配置の説明を追加・確認。 |
| UX-002 | テストで検証済み | 既存の単発配置reducerテストとブラウザ実操作で、配置直後に選択状態になることを確認。 |
| UX-003 | テストで検証済み | 連続配置Action、配置待機文言、連続配置バッジを確認。 |
| UX-004 | 実装済みだが人間の確認待ち | Shift解除時の待機解除ロジックは実装済み。実機キーボードで押下・解除の一連の操作を確認する。 |
| UX-005 | テストで検証済み | calibrationDistanceLabelの単体テストと校正ダイアログのブラウザ表示を確認。 |
| UX-006 | テストで検証済み | モード表示名の単体テスト、aria-pressed、ブラウザでの範囲選択表示を確認。 |
| UX-007 | 実装済みだが人間の確認待ち | 未校正・未保存・ロック・連続配置・現在モードの表示を実装。画面全体の視認性は実機／実画面で確認する。 |
| UX-008 | 実装済みだが人間の確認待ち | Pointer Events、フォーカス可能キャンバス、900px以下のタッチサイズを実装。iPad Safari実機で読込・ピンチ・移動・回転を確認する。 |
| UX-009 | テストで検証済み | Undo/Redo既存テストと、矢印キー相当のNUDGE_SELECTED reducerテストを確認。 |
| UX-010 | テストで検証済み | npm test、npm run typecheck、npm run buildが成功。 |

## Phase 5で追加した問題と対応

| ID | 優先度 | 発見した問題 | 対応状況 |
|---|---|---|---|
| UX-PHASE5-001 | P1 | 3D床へ背景画像をそのまま貼ると、校正値や90度回転後の実寸と一致しない可能性があった。 | 修正済み。校正済みmmで背景平面の幅・奥行・原点を生成し、2Dと同じ座標へ配置。 |
| UX-PHASE5-002 | P1 | 閉じた壁を登録しても、3D押し出しが最後の点から始点へ戻らなかった。 | 修正済み。closed=trueのポリラインは閉じる線分も生成し、テストを追加。 |
| UX-PHASE5-003 | P2 | 一人称視点を任意位置へ置く操作がキーボード中心で、タッチ利用者に分かりにくかった。 | 修正済み。3Dミニマップを追加し、タップ位置へ一人称視点を移動できるようにした。 |
| UX-PHASE5-004 | P2 | 視点高さプリセットを変更しても、一人称カメラへ反映されるのが席視点実行後だった。 | 修正済み。プリセット／任意値の変更を現在の一人称カメラへ即時反映。山台の段高も維持する。 |
| UX-PHASE5-005 | P1 | ミニマップで視点位置を移動した後、タッチブラウザのジェスチャー処理により中央3D画面の視線回転が不安定になる余地があった。 | 修正済み。Three.js canvasへPointer Eventsを直接登録し、touch-action:noneとPointer Captureを併用して、マウス／タッチのドラッグ回転を確実に受け取る。 |
| UX-PHASE5-006 | P0 | WebGL初期化・描画・コンテキスト喪失時に3D画面が利用不能になり、2D編集へ戻る導線もなかった。 | 実装済みだが人間の確認待ち。lazy importのErrorBoundary、WebGL初期化／描画／context lostの復帰表示、2Dへ戻るボタンを追加。WebGL無効環境での実機確認が必要。 |
| UX-PHASE5-007 | P1 | 3D画面の上に2Dツールバーが残り、編集できる場所・戻り方が分かりにくかった。 | 修正済み。3D表示中は2Dツールバーを隠し、3D側に「2D編集へ」導線を常時表示。ブラウザ確認済み。 |
| UX-PHASE5-008 | P1 | 3D床の背景画像が切り抜き・90度回転・背景／レイヤー非表示を反映せず、2Dと実寸がずれる可能性があった。 | 実装済みだが人間の確認待ち。校正済みmmの平面へeffective cropと回転を適用し、CanvasTextureへ変換。非表示時は床背景を生成せず、読込失敗時は実寸グリッドへ戻す。実図面で2Dとの目視比較が必要。 |
| UX-PHASE5-009 | P1 | Pointer Capture解除、コンテナリサイズ、3D再入場時のリソース解放が不十分で、ドラッグ停止・表示崩れ・長時間利用時の負荷につながる余地があった。 | 実装済みだが人間の確認待ち。lostpointercapture／pointercancel、ResizeObserver、モデルのgeometry/material破棄を追加。ブラウザ再入場とドラッグは確認済み。iPad長時間操作・多数オブジェクトは人間確認が必要。 |
| UX-PHASE5-010 | P2 | 視点の向き・初期位置・背景画像床の利用可否が画面だけでは把握しにくかった。 | 修正済み。視点リセット、ミニマップの視線方向線、モード別aria/help、背景画像未読込表示を追加。ブラウザ確認済み。 |

## 2026-07-19 3D監査の追加実装

- `src/App.tsx`: 3D lazy importのSuspense中に戻れる読み込み表示と、予期しない3D例外から復帰するErrorBoundaryを追加。3D表示中は2Dツールバーを表示しない。
- `src/components/Viewer3D.tsx`: WebGL初期化・描画・context lostのエラー表示、2D復帰、背景CanvasTextureの切り抜き／回転、視点リセット、Pointer Capture／ResizeObserver／WebGLリソース解放を追加。
- `src/core/scene3d.ts`: 背景のeffective crop・回転・表示状態・背景レイヤー状態をmm正本から3Dモデルへ引き継ぐ。
- `src/core/scene3d.test.ts`: 背景切り抜き・回転・可視性のAC-201回帰テストを追加。
- `src/phase5.css`: 3D読み込み中・エラー時の復帰可能な表示と44px以上の戻るボタンを追加。
- `docs/UX監査_2026-07-19.md`: コード精読・ブラウザ実測で発見した3Dおよび周辺UIの問題をP0〜P3で記録。今回対象外のP1/P2は将来対応として残す。

## Phase 5の実装内容

- src/state/appState.ts: 壁の追加、壁全体／頂点の編集、頂点削除、壁削除、舞台前端設定をActionとして追加。2点未満、重複ID、非有限値を拒否／正規化しUndo履歴へ登録。
- src/components/CanvasStage.tsx、src/components/Toolbar.tsx: 背景上のPointer Eventsによる壁トレース、2点以上で確定、高さ6000mm初期値、トレース中の頂点数・確定・やり直し表示。
- src/components/PropertyPanel.tsx: 壁の高さ、閉じる設定、各頂点のX/Y、頂点追加／削除、壁削除、舞台前端Yの編集。
- src/core/scene3d.ts: mm正本から壁・オブジェクト・山台上の配置・アバター・背景平面・一人称初期値を生成。Three.jsに依存しない純粋ロジック。
- src/components/Viewer3D.tsx: React.lazyで遅延読み込みするThree.js表示。俯瞰OrbitControls、一人称、席視点、目線高さ、床表示、背景テクスチャ、ミニマップ、Three.js canvasへのPointer Events、WASD／矢印移動。
- src/phase5.css: 壁編集UI、3D画面、ミニマップ、iPad相当幅のレスポンシブ／タッチ領域。
- three / @types/three: 壁・箱・円柱・カメラ・OrbitControlsを実装するため追加。代替の自前WebGLは保守・テスト範囲が大きくなるため採用しなかった。Three.jsはViewer3Dの遅延チャンクへ分離。

## 今回の変更ファイルと動作変更

- `src/state/appState.ts`: モード表示名と `NUDGE_SELECTED` Actionを追加。既存のmm座標・end座標・ロック判定・Undo履歴を通る。
- `src/App.tsx`、`src/components/CanvasStage.tsx`: 選択中の矢印キー移動、キャンバスフォーカス、入力欄・ボタンへの誤発火防止を追加。
- `src/components/Toolbar.tsx`: 現在モード、配置待機、連続配置の表示、モードARIA状態、未保存のJSON読込確認を追加。
- `src/components/LibraryPanel.tsx`、`src/core/presets.ts`、`src/components/CalibrationDialog.tsx`: 配置手順・標準初期値・mm単位・校正距離の意味を明示。
- `src/components/ExportDialog.tsx`: 1:50／1:100／フィットと100%印刷の説明を追加。
- `src/phase3.css`、`src/phase4.css`: 900px以下の縦レイアウトと44pxタッチターゲットを追加。
- `src/core/calibration.test.ts`、`src/state/appState.test.ts`、`src/state/appState.phase2.test.ts`: 校正ラベル、モード表示、mmナッジの回帰テストを追加。
- `README.md`、`HANDOFF.md`: 実装範囲、UX監査結果、受入状況、手動確認事項を更新。

## Phase 4の実装内容

- レイヤー: 背景、オブジェクト、注釈の表示／非表示、ロック、アクティブレイヤー。
- 注釈: テキスト、線、矢印、矩形、円、寸法線。終点もmmで保存し、選択・PNG/PDF出力に対応。
- スナップ: グリッド、他オブジェクト中心、舞台中心線。間隔・閾値はプロジェクトへ保存。
- 行列配置: 行数、列数、左右／上下間隔を指定し、元オブジェクトを基準に複製。
- プルト弧状配置: 中心、半径、開始／終了角度、プルト数、プルト間隔を指定。生成後は個別選択・編集可能。
- 山台関連付け: プロパティの選択欄から onRiserId を手動指定。
- 追加プリセット: 演台、アップライトピアノ、チェレスタ、バスドラム、ヴィブラフォン、シロフォン／グロッケン、チャイム、ドラムセット、ティンパニ23/32インチ、アンプ／スピーカー。

## Phase 5の自動検証結果

- src/core/scene3d.test.ts: AC-201（壁・背景・可視性・舞台前端）、AC-202（席視点）、AC-203（山台高さ）、AC-204（視線回転角）。
- src/state/appState.phase5.test.ts: 壁トレースのAction、頂点編集／削除、既定高さ、Undo履歴、入力正規化。
- ブラウザ: 3Dビュー、俯瞰／一人称、視点高さ変更、選択席視点、壁トレース2点入力、ミニマップ移動後の中央画面ドラッグ回転を確認。今回も背景画像床、2D復帰→3D再入場、俯瞰ドラッグ、視点リセットを確認し、新規console error/warnは0件。

## 自動検証結果

- npm test: 21テストファイル、86テスト通過。
- npm run typecheck: 通過。
- npm run build: 通過。
- ビルド時のチャンクサイズ警告はPDF.jsを同梱しているためで、ビルド失敗ではない。
- PDF.jsのNodeテスト時にlegacy build利用の警告が出るが、テスト失敗ではない。
- npm install時にnpm auditの脆弱性警告（5件）が表示された。依存更新は本Phaseの範囲外として保留。
- 開発サーバー: http://127.0.0.1:5173/

## 人間による確認事項

1. 校正済みプロジェクトでPNGを出力し、背景・配置物・注釈の位置と編集UI非表示を確認する。
2. A3横・1:100 PDFを出力し、PDFビューアで1ページ、注釈、出力情報欄を確認する。
3. 同PDFをプリンタ倍率100%で印刷し、1820mm基準線が18.2mm±1mmか実測する。
4. 実機iPad Safariで、背景読込→校正→配置→移動→回転→複製→注釈→PDFの順に確認する。
5. レイヤーの表示／ロック、14プルト弧状配置、行列配置、山台関連付けをブラウザで確認する。
6. 校正済みの実図面で壁トレースを行い、頂点の追加・X/Y移動・削除・高さ変更・閉じた壁を確認する。
7. そのプロジェクトを3Dへ切り替え、壁の高さ、背景画像の90度回転／切り抜き、配置物の実寸関係、山台上アバターを確認する。
8. 椅子を選択して「この席から見る」を実行し、山台上では目線が段高分だけ上がることを目視確認する。
9. iPad Safari実機で、俯瞰のドラッグ／ピンチ、一人称の片指ドラッグ／タップ移動、ミニマップ移動を確認し、オブジェクト200個＋アバター100体＋壁で30fps以上を実測する。
10. WebGL無効化またはcontext lostを発生させ、3Dエラー表示から2D編集へ戻れることを確認する。
11. 実図面の切り抜き・90度／270度回転・背景レイヤー非表示を使い、2D背景と3D床の向き・範囲・実寸が一致することを目視確認する。
12. iPad Safari実機で、ドラッグ中の指追加・指離し・画面外離脱・回転／ピンチ後の再操作を確認する。
13. 3Dの開閉を複数回繰り返し、多数オブジェクトを表示して、キャンバスの重複・メモリ増加・30fps以上を確認する。
## 2026-07-19 一人称カメラ回転修正・GitHub Pages公開

| ID | 優先度 | 問題 | 対応状況 |
|---|---:|---|---|
| UX-PHASE5-011 | P1 | ミニマップの矢印だけが回転し、実際の3D画面の視線が回転しなかった。描画ループのOrbitControls更新が一人称カメラのlookAtを上書きしていた。 | 修正済み。firstPerson中はOrbitControls更新を止め、poseRefを正本として一人称カメラ姿勢を毎フレーム再適用。ブラウザで一人称ドラッグとconsole error/warn 0件を確認。 |

### 変更内容

- src/core/scene3d.ts: 一人称中にOrbitControlsを更新しないルールを純粋関数化。
- src/components/Viewer3D.tsx: 一人称描画時のカメラ姿勢上書きを防止。
- src/core/scene3d.test.ts: 一人称カメラ回転ルールの回帰テストを追加。
- .github/workflows/deploy-pages.yml: mainへのpushでGitHub Pagesへ静的ビルドを配信。
- README.md: 外出先確認用URLと端末内保存／JSON持ち運びの注意を追加。

### 外出先確認用URL

- https://fd19941005-code.github.io/stage-layout-app/
- 初回公開または更新直後はGitHub Actionsの完了まで待つ。アプリは認証なし公開で、URLを知る人は閲覧できる。
- 配置データはGitHubへアップロードされず、端末ごとのIndexedDBに保存される。別端末へ移す場合はJSON保存／復元を使う。

### 今回の自動検証

- npm test: 21ファイル、87テスト通過。
- npm run typecheck: 通過。
- npm run build: 通過。

## 2026-07-25 椅子多列円弧配置(FR-064 / AC-102)

選択した指揮台を円弧中心として、列ごとに指定した脚数の標準椅子を同心円弧上へ一括配置する機能を追加した。
標準椅子の円形化は着手前に完了済みのため、本機能では椅子シンボル・SVG・表示資産・プリセット寸法・ラベル仕様を一切変更していない。

### 変更内容

- src/core/arrangement.ts: 多列円弧の型と純粋関数を追加(`podiumExtentAlongDirection` / `chairArcFirstRowRadiusMm` / `chairArcRowAnglesDeg` / `createChairArcRowPlacements` / `createChairArcRowObjects` / `detectChairArcOverlaps` / `detectChairArcObjectOverlaps` / `detectChairArcOffStagePlacements` / `validateChairArcRowsOptions` / `chairArcLaunchIssue`)。既存の行列配置・プルト弧状配置は無変更。
- src/state/appState.ts: `ADD_CHAIR_ARC_ROWS` Actionとreducer処理を追加。全椅子を1回の`commitProject`で追加し、生成した全IDを選択状態にする。`createPresetObject`をexportし、単体配置と同じ生成経路をプレビューでも使えるようにした。
- src/components/ChairArcRowsDialog.tsx: 入力パネルを新規追加。キャンバス上で方向指定・端ハンドル操作を続けられるよう、`dialog-backdrop`を使わない非モーダルの浮動パネルとした。
- src/components/CanvasStage.tsx: 確定前プレビュー(仮椅子・円弧補助線・中心方向線・左右端ハンドル)とPointer Eventsによる方向指定／円弧角／距離の操作を追加。仮椅子は既存の`#stage-symbol-chair`を`<use>`で再利用し、専用の円描画は作っていない。
- src/components/PropertyPanel.tsx: 「一括配置」へ起動ボタンと不可理由の表示を追加。
- src/App.tsx: 確定前セッションを一時state(`ChairArcSession`)として保持。Escで破棄し、基準指揮台が消えた場合も自動破棄する。
- src/phase4.css: パネル・可変列・ハンドル・警告表示のスタイルを追加。
- src/core/arrangement.chairArc.test.ts / src/state/appState.chairArc.test.ts: 幾何・警告・起動条件・reducerのテストを追加。

### 距離のドラッグ調整(利用者要望による追加)

数値入力だけだった距離を、キャンバス上でも変えられるようにした。

- 各列の円弧補助線そのものを当たり判定にする。線に沿って画面44px幅(最低300mm)の透明ストロークを重ねているため、ハンドル同士が重ならず、列間隔を詰めてもタップ領域を確保できる。中心方向線上にはつまみ位置を示す小円を描画する。
- 1列目の弧を引くと`firstGapMm`、2列目以降の弧を引くと`rowGapMm`が変わる。掴んだ列までの半径差を列数で割るので、どの列を引いても列間隔の意味は同じ。
- 換算は`chairArcFirstGapFromRadiusMm` / `chairArcRowGapFromRadiusMm`としてcoreの純粋関数へ置き、コンポーネントには座標計算を書いていない。
- ドラッグ結果だけ10mm刻み(`CHAIR_ARC_DRAG_STEP_MM`)へ丸め、下限10mm(`CHAIR_ARC_MIN_GAP_MM`)で止める。数値入力の値は丸めない。
- ドラッグと数値欄は双方向に同期する。既定値は仕様どおり1820mm／910mmのまま変更していない。

### 仕様上の決定

- 中心方向の初期値は指揮台の`rotationDeg - 90`とし、回転0では画面上方向になる(取得不能時の既定と一致)。
- 椅子半径は`max(widthMm, depthMm) / 2`をchairプリセットの実寸から取得する。値のハードコードはしていない。
- 舞台外警告は、明示的な有効舞台範囲として`project.stageFront`(舞台前端)が設定されている場合だけ判定する。背景画像の外周は境界とみなさない。
- 円弧パラメーターは保存しない。schemaVersionは変更していない。

### 自動検証

- npm test: 40ファイル・213テスト通過(うち本機能の新規33テスト: `arrangement.chairArc` 23、`appState.chairArc` 10)。
- npm run typecheck: 通過。
- npm run build: 通過。チャンクサイズ警告は従来どおりPDF.js同梱によるもの。
- ブラウザ確認: 指揮台選択→起動→プレビュー14脚・円弧補助線2本・ハンドル3個(実タップ領域44px)・1列目半径2495mm表示、円弧角を10度へ狭めると重なり警告が出ても「配置する」は有効、方向／端ハンドルのドラッグでプレビューが追従、キャンセルでプレビュー全消去。操作中も保存状態は「保存済み」のままでProjectとUndo履歴は変化しなかった。新規のconsole error/warnなし。

### 人間による確認事項

14. 実際の舞台図面で6脚・8脚・10脚の3列を配置し、左右端が放射方向上で揃うことを目視確認する。
15. 指揮台から1列目までの空きが中心方向上で1820mm、列中心間隔が910mmになることを測定ツールで実測する。
16. 指揮台を45度回転した状態で起動し、1列目半径が回転後外形端基準になっていることを実測する。
17. 配置後に端の椅子だけを手動移動・削除し、Undo/Redoが一括単位で動作することを確認する。
18. 生成された椅子が単体配置した標準椅子と同じ円形シンボル・寸法・ラベルで表示され、PNG/PDF出力にプレビュー用ガイドやハンドルが含まれないことを確認する。
19. iPad Safari実機で、方向指定・端ハンドル・円弧線の距離ドラッグ・脚数変更・配置確定まで完了できることを確認する。
20. 列間隔を実配置で詰めた／広げた状態を見比べ、既定910mm(半間)が実用値として妥当か判断する。狭い場合は既定値の変更が必要。

## 2026-07-25 弦楽器テンプレ配置(FR-062 / AC-STR-001〜012)

オーケストラ弦五部の椅子・譜面台を、編成人数・セクション順・コントラバス位置・配置間隔から一括生成する機能を追加した。
既存の`createPultArcObjects`は単一円弧・固定2脚・セクション別複数列非対応のため配置関数としては使わず、1プルト生成処理だけを共通化して再利用した。

### 変更内容

- src/core/arrangement.ts: 1プルト生成を純粋関数`createPultUnitObjects`(`PultUnitSpec` / `PultUnitIdFactory`)として抽出し、`createPultArcObjects`から呼ぶよう置き換えた。ラベル・ID採番・zIndex・回転角の外部仕様は不変で、既存テストも無変更で通過する。
- src/core/stringSectionTemplate.ts(新規): 型・編成型プリセット・セクション順プリセット・`pultCountForPlayers` / `distributePultsToRows` / `computeStringSectionLayout` / `createStringSectionTemplateObjects` / `moveSectionOrder` / `normalizeSectionOrder` / `validateStringSectionTemplateOptions` / `stringSectionTemplateLaunchIssue` / `stringTemplateWarnings` / セッション生成とオプション組み立て。すべてmm単位の純粋関数。
- src/state/appState.ts: `ADD_STRING_SECTION_TEMPLATE` Actionを追加。校正・レイヤー・入力値を確定時に再検証し、全オブジェクトを1回の`commitProject`で追加して全生成物を選択状態にする。
- src/components/StringSectionTemplateDialog.tsx(新規): 入力パネル。ライブプレビューを見ながら操作できるよう非モーダルの浮動パネルとした。
- src/components/CanvasStage.tsx: `previewObjects`を描画専用データとして受け取る`renderTemplatePreview`を追加。座標計算はcore側で完結し、コンポーネントには一切書いていない。
- src/components/PropertyPanel.tsx: 「一括配置」へ起動ボタンと不可理由の表示を追加。
- src/App.tsx: 確定前セッション`StringSectionTemplateSession`を一時stateで保持し、`useMemo`でプレビューSceneObjectと警告を算出。Escで破棄する。
- src/phase4.css: パネル(`.chair-arc-panel`と共用)・人数グリッド・順序変更・プレビューのスタイルを追加。
- src/core/stringSectionTemplate.test.ts / src/state/appState.stringTemplate.test.ts: 受入条件のテストを追加。

### 仕様上の決定

- 接線座標`sMm`は仕様13.4の式`angleRad = PI/2 + sMm / radiusMm`に合わせ「図面左へ正」とした。図面左から右へのセクション順は`sMm`の大きい側から並べる。
- 回転角はシンボル基準方向-Yに対して`角度 - 90度`とし、奏者位置・譜面台が指揮者方向を向くようにした。既存`createPultArcObjects`は`角度 + 90度`(弧中心と反対向き)だが、外部仕様維持のため変更していない。
- **最前列半径の下限**: 仕様13.4の同心円弧モデルでは、弦4セクションの接線方向必要幅(12型で13000mm)に対して最前列距離が小さいと扇形が180度を超え、奏者が指揮者を回り込む。仕様11.1の初期値1820mmでは409度になり配置が成立しないため、扇形が180度に収まる最小半径を下限とし、下限を適用した場合は警告文で拡大後の半径と必要な最前列距離を提示する。半径以外の計算式・初期値・入力制約は仕様どおり。
- コントラバスは`contrabass-stool`プリセットを使い、プルト内椅子間隔は仕様11.2の内部固定値600mmをそのまま適用した。プリセット幅900mmに対して間隔が狭いため、同一プルト内および隣接プルト間で占有域が重なる(下記の人間確認事項22)。
- プルト番号はセクションごとに前列の図面中央寄りから外側へ、次に後列へ進む。表・裏は図面中央から遠い側を表とした。ラベルのみで役職管理はしない。
- テンプレート設定は保存しない。`SceneObject`・`schemaVersion`・`deserializeProject`は変更していない。外部ライブラリも追加していない。

### 自動検証

- npm test: 42ファイル・275テスト通過(うち本機能の新規59テスト: `stringSectionTemplate` 47、`appState.stringTemplate` 12)。
- npm run typecheck: 通過。
- npm run build: 通過。チャンクサイズ警告は従来どおりPDF.js同梱によるもの。
- ブラウザ確認(Chromium 1280x720): 未校正時はボタン無効で理由表示、校正後に起動して12型プレビュー60個(奏者40・譜面台20)を表示。人数変更で即時再計算(ヴィオラ8→14で69個)。この間`project.objects`は0個、履歴1/50、保存状態が変化せず、プレビュー上の`elementFromPoint`はキャンバスを返す(`pointer-events: none`)。「右へ」でVa・Vcを入れ替えるとプリセット表示が「Va・Vc逆」へ、さらに動かすと「カスタム」へ変化。順序変更ボタンの実高さ44px。配置確定で60オブジェクト生成・60個選択・履歴2/50、1回のUndoで0個、1回のRedoで60個。新規のconsole errorなし。

### 人間による確認事項

21. 実際のホール図面で12型を配置し、最前列距離を6000〜8000mm程度へ上げた状態の扇形が実運用に合うか確認する。初期値1820mmでは最前列半径が自動拡大される(上記の仕様上の決定)ため、初期値自体を見直すか判断する。
22. コントラバスの占有域(900×1200mm)が、プルト内椅子間隔600mm・プルト間隔1100mmで重なることを目視確認し、Cb専用の間隔設定を追加するか判断する。
23. 8型・16型・カスタム(奇数人数)で生成し、片プルトの椅子がプルト中心にあり、譜面台が奏者より指揮者側にあることを実測する。
24. 対向配置(1st Vn → Va → Vc → 2nd Vn)とVa・Vc逆配置を生成し、実際の楽団の並びと一致するか確認する。
25. コントラバス位置4種類を切り替え、図面左後方・右後方が弦4セクションの外側後方へ、チェロ後方・ヴィオラ後方が対象セクション後方へ来ることを目視確認する。
26. 配置後の奏者位置・譜面台を個別に移動・回転・削除・グループ化でき、PNG/PDF出力へプレビュー表示が含まれないことを確認する。
27. iPad Safari実機で、ドラッグを使わず「左へ」「右へ」ボタンのみでセクション順を変更し、人数・間隔を入力して配置確定まで完了できることを確認する。
28. 背景範囲外へはみ出す配置とオブジェクト数500超の配置で、警告が出ても確定できることを確認する。

## 2026-07-25 弦楽器テンプレ配置の方向・配置領域修正

配置結果が客席側へ展開される問題、左右が意図と逆になる問題、各セクションが1本の長い円弧になる問題を修正した。
新規機能追加ではなく配置計算の作り替えで、編成・生成対象・入力項目・プレビュー・確定操作・Undo/Redo・mm正本の基本仕様は維持している。
参考配置画像は本セッションへ添付されなかったため、修正指示の文面(第1Vn=図面左前方、Va=図面右前方、Vc=中央右やや後方、Cb=図面右後方、前後に厚みのあるまとまり、千鳥、浅い局所円弧)を正本として実装した。

### 配置方向が逆だった原因

旧実装は`angleRad = PI/2 + sMm / radiusMm`として**Y増加方向を舞台奥と固定**していた。
舞台前端が指揮者よりY値の大きい図面(舞台が下・客席が上)では、この固定値がそのまま客席側になる。
併せて接線座標`sMm`を「図面左へ正」と定義していたため左右の符号が直感と逆で、
セクション順プリセットも旧仕様の`1st Vn→2nd Vn→Va→Vc`(Vaが中央右)だった。

### 変更内容

- src/core/arrangement.ts: `PultUnitSpec`へ`rotationForPoint`を追加し、1プルト内でもオブジェクト位置ごとに回転角を決められるようにした。`createPultArcObjects`は未指定のまま呼ぶため挙動は不変。
- src/core/stringSectionTemplate.ts: 配置計算を同心円弧モデルから領域モデルへ作り替えた。
  - `stageFrameFor(conductor, stageFrontYMm)`: 舞台前端から指揮者へ向かう向きを舞台奥方向`depth`とし、直交して常に図面右を向く`lateral`を返す。Y方向は固定しない。
  - `stageFramePointMm`: `(lateralMm, depthMm)`のローカル座標をmm座標へ変換する。配置計算はすべてローカル座標で行う。
  - `facingRotationDeg(from, toConductor)`: シンボル基準方向-Yに対する指揮者向きの回転角。
  - `stringSectionRegions`: セクションごとの配置領域(左右アンカー・前後アンカー・左右の広がり・舞台奥の広がり・基準角度・浅い円弧の強さ・最大列数・後列のずらし量と方式)を返す。
  - `distributePultsAcrossRows` / `distributePultsToRows`(1列最大4プルトへ変更) / `doubleBassRowCount`。
  - `computeStringSectionLayout`: 領域内で列・千鳥・浅い円弧を組み立てる。旧`stringTemplateFirstRowRadiusMm`と扇形180度クランプ、`firstRowRadiusExpanded`警告は不要になったため削除した。
  - `stringTemplateWarnings`: 間隔不足警告(`pultPitchTooNarrow` / `rowGapTooNarrow`)を追加。
  - `StringSectionTemplateOptions`へ`stageFrontYMm`を追加(保存スキーマではなく計算入力)。
- src/state/appState.ts: 変更なし(`ADD_STRING_SECTION_TEMPLATE`はoptionsをそのまま使うため、プレビューと確定が同一計算になる)。
- src/App.tsx: `stringSectionTemplateOptionsFromSession`へ舞台前端Yを渡し、警告算出へoptionsを渡すよう更新。
- src/components/StringSectionTemplateDialog.tsx: 概要表示を最前列半径から最前列距離・最大列数へ変更。
- src/core/stringSectionTemplate.test.ts / src/state/appState.stringTemplate.test.ts: 必須テスト24項目へ合わせて全面的に書き替えた。

### 舞台奥方向の算出

指揮者は必ず舞台上にいるため、舞台前端から指揮者へ向かう向きが舞台奥方向になる。

- `stageFront.yMm`が設定済みかつ指揮者Yと異なる: `depth = (0, sign(conductorY - stageFrontY))`
- 未設定・同値・非有限: `depth = (0, +1)`(従来の既定と一致)
- `lateral`は`depth`を-90度回し、符号をそろえて常に図面右(+X)を向かせる

前列から後列へは`depth`方向へ列間隔を加算する。列内の左右は`lateral`方向へ展開する。

### 各配置プリセットのアンカー位置

左右アンカーは`sectionOrder`の順に領域幅ぶんタイルし、全体を指揮者正面で左右対称にする。
前後段は並びの**外側セクションが前方段(段差0)**、**内側セクションが後方段(段差=列間隔)**になる。
このため順序を入れ替えると左右アンカーだけでなく前後段も実際に入れ替わる。

12型・座標基準・最前列距離1820mmでの実測値(指揮者を原点、左右は図面右へ正、前後は舞台奥へ正、単位mm):

| プリセット | 図面左 → 右の順と(左右アンカー, 最前列の奥行き) |
|---|---|
| 標準 | 1st Vn(-5811, 1534) → 2nd Vn(-1782, 2367) → Vc(+1459, 2337) → Va(+5533, 1541) → Cb(+9467, 4198) |
| Va・Vc逆 | 1st Vn(-5811, 1534) → 2nd Vn(-1782, 2367) → Va(+1996, 2352) → Vc(+6082, 1532) → Cb(+9467, 4198) |
| 対向 | 1st Vn(-5811, 1534) → Vc(-1991, 2367) → Va(+1996, 2352) → 2nd Vn(+6303, 1532) → Cb(+9462, 3271) |

### 複数列への分配

`pultCount = ceil(奏者数 / 2)`、`列数 = ceil(pultCount / 4)`、余りは前列から1プルトずつ割り当てる。
1〜4プルト=1列、5〜8=2列、9〜12=3列、13以上は4プルト/列で列数を増やす。
5→3+2、6→3+3、7→4+3、8→4+4、9→3+3+3、10→4+3+3、11→4+4+3、12→4+4+4、13→4+3+3+3。

### 千鳥配置の計算方法

列ごとの生のオフセットを`(columnIndex - (rowPultCount - 1) / 2) × プルト間隔 + ずらし`で求める。
ずらしは千鳥方式で`奇数列のみ プルト間隔 × 0.5`(1列目0・2列目0.5・3列目0)。
最後に領域内の最小値と最大値の中点を全列から引いて中央へ寄せ直すため、列間の位相差0.5プルトを保ったまま領域から片寄らない。
各列は完全な直線ではなく、左右へ離れるほど舞台奥へ`arcStrength × 左右オフセット² / (2 × アンカー距離)`だけ下げた浅い円弧にする(`arcStrength = 0.35`)。
指揮者中心の円弧の弧高を0.35倍した2次近似なので、外側端が舞台奥へ跳ね上がらない(12型1st Vn前列の弧高は約80mm)。

### コントラバスの配置方法

通常弦とは別規則。

- 奏者位置は`contrabass-stool`プリセット(通常椅子へ置き換えない)。
- 列数は1列あたり最大3人を目安に`ceil(人数 / 3)`、上限4列。1〜3人=1列、4〜6人=2列、7人以上=3列。
- プルト内椅子間隔とプルト間隔はスツール実寸(900mm)から重ならない値へ広げる(間隔1000mm、プルト間隔1900mm)。ハードコードせずプリセット実寸から導く。
- 後列のずらしは千鳥ではなく斜め方式で、列数に比例して図面外側へずらす短い斜め列にする。
- 舞台奥方向は弦4セクションの最後列より`cbRearGapMm`(1000mm)だけ後方。
- 位置4種類はアンカー座標へ反映する。図面右後方=弦全体の右端+セクション間隔+Cb半幅、図面左後方=左端-同、チェロ後方・ヴィオラ後方=対象セクションの左右アンカー。既定は図面右後方。

### 自動検証

- npm test: 42ファイル・**293テスト通過**(本機能は`stringSectionTemplate` 62、`appState.stringTemplate` 15)。既存テストの削除・skipなし。
- npm run typecheck: 通過。
- npm run build: 通過。チャンクサイズ警告は従来どおりPDF.js同梱によるもの。
- ブラウザ確認(Chromium 1280x720、舞台前端Y=1500・指揮者(5000, 2400)):
  - 12型標準で60オブジェクト。全生成物が指揮者より舞台奥側(depth 1534〜5992mm)。
  - 指揮者Yを舞台前端より客席側(500)へ変えると舞台奥方向が反転し、全生成物がY<500へ出る。左右の並びは不変。
  - 全60オブジェクトの指揮者向きコサインが1.000000000(誤差なし)。
  - 3プリセットの左右・前後アンカーが上表どおり。Va・Vc逆では左右と前後段の両方が入れ替わる。
  - プレビューのtransform文字列60件と確定後60件が完全一致。
  - 確定で60個生成・60個選択、1回のUndoで0個、1回のRedoで同一transformへ復元。プレビューは確定後に消える。
  - 既定値では警告なし。プレビュー中はproject.objects 0個・履歴・保存状態が不変。新規のconsole errorなし。

### 人間による確認事項

29. 実際のホール図面で12型を配置し、各セクションのまとまりが参考配置と一致するか目視確認する。とくに第1ヴァイオリンとヴィオラの前列が舞台前縁に沿っているか。
30. 12型のヴィオラ8人(4プルト)とチェロ6人(3プルト)は指定の列分配表どおり1列になる。参考配置で2列にしたい場合は列分配表の見直しが必要か判断する。
31. 内側セクション(第2ヴァイオリン・チェロ)の後方段差が列間隔と同じ910mmで足りるか、実配置で確認する。
32. 浅い円弧の強さ0.35が参考配置の曲率と合うか確認する。強くしたい場合は`STRING_TEMPLATE_ARC_STRENGTH`のみで調整できる。
33. コントラバスの斜め列の向きと間隔(1900mmピッチ)が実運用に合うか確認する。
34. 舞台前端を設定していないプロジェクトではY増加方向を舞台奥として扱うため、図面が上下逆の場合は舞台前端Yを先に設定する運用でよいか確認する。
35. 対向配置を実際の楽団の並びと突き合わせる。中央のVc(左)・Va(右)の前後段差が実運用に合うか確認する。
36. iPad Safariで、ドラッグを使わず「左へ」「右へ」ボタンのみでセクション順を変更し、配置確定まで完了できることを確認する。
37. 生成物を個別に移動・回転・削除・複製・グループ化でき、PNG/PDF出力へプレビュー表示が含まれないことを確認する。

## 2026-07-25 弦楽器テンプレ配置を共通最前列円弧モデルへ変更

セクション別配置＋左右タイル方式を廃止し、弦全体共通の最前列円弧を先に作るモデルへ配置アルゴリズムを変更した。
併せて舞台奥方向の推測をやめ、客席側へ生成される不具合を直した。
仕様書v1.2と参考配置画像は本セッションへ添付されなかったため(`docs/`にあるのは`弦楽器テンプレ配置_仕様書_v1.1.md`)、
修正指示の本文を正本として実装した。v1.1と異なる点は下記「仕様上の決定」に記載する。

### 客席側へ生成されていた原因

`stageFrameFor()`が舞台奥方向を`depth = (0, sign(conductorY - stageFrontY))`として
**stageFront.yMmとの大小関係から推測**していた。初期セッションは`conductorY = stageFrontY + 900`だったため
符号が常に+1になり、`depth = (0, +1)` = 画面下 = 客席側へ全体が展開されていた。
現行アプリは画面上が舞台奥(Y負方向)であり、`stageFront`は任意角度の前端線も法線も持たないため、
この推測自体が成立しない。

### 共通最前列円弧の計算方法

`computeStringFrontArc(options)`。

1. 半径 `R1 = 最前列距離`(指揮台基準では `指揮台奥行き/2 + 最前列距離`)。
2. 有効セクション(人数1以上)だけを`sectionOrder`の順に取り、隣接する先頭プルトの弧長間隔を
   `プルト間隔 + セクション間隔`とする。角度ステップは `stepRad = (プルト間隔 + セクション間隔) / R1`。
3. 全開き角 `(有効数 - 1) × stepRad` が上限120度(`STRING_TEMPLATE_MAX_FRONT_ARC_RAD`)を超える場合だけ
   等比で詰め、比率を`frontArc.scale`として保持する(0.9未満で警告)。
4. 先頭プルトのアンカー角は `(index - (有効数 - 1) / 2) × stepRad`。人数0のセクションを除外したうえで
   残りを円弧中央へ再配置する。
5. 座標は極座標から `舞台奥ベクトル(0, -1)` と `図面右ベクトル(1, 0)` で合成する(`stagePolarPointMm`)。
   4セクションがすべて2人以上なら最前列は各セクション2人ずつ、合計8人になる。

### 各セクションの後列分配方法

`distributeStringSectionPults(pultCount)`。前列は必ず先頭プルト1つで、残りを後列へ分配する。
後列は1列最大4プルトで、余りを後方の列へ寄せて後ろほど広い扇形にする。

1→[1] / 2→[1,1] / 3→[1,2] / 4→[1,1,2] / 5→[1,2,2] / 6→[1,2,3] / 7→[1,3,3] / 8→[1,3,4] / 9→[1,4,4]
10→[1,3,3,3] / 13→[1,4,4,4] / 14→[1,3,3,3,4]

12型の実測: 1st Vn `1+2+3` / 2nd Vn `1+2+2` / Va `1+1+2` / Vc `1+2`。旧仕様の3+3均等分割は使用しない。

### 後方扇形と千鳥配置の計算方法

`stringRowPlan()`。列`r`のプルトを、先頭プルトのアンカー角からの弧長オフセットで求める。

- 列内: `(columnIndex - (列プルト数 - 1) / 2) × プルト間隔`。
- 外側ずらし: 列中心を `outwardSign × r × (プルト間隔 × 0.25)` だけセクション外側へずらす。
  `outwardSign`はアンカー角の符号(円弧中央から遠い側)。半径は `R1 + r × 列間隔`。
- 千鳥: 目標位相を偶数列0・奇数列0.5プルトとし、列内プルト数の偶奇から生じる自然な位相
  (偶数個なら0.5、奇数個なら0)を打ち消して目標位相へそろえる。これにより列プルト数が
  変わっても隣接列が必ず半プルトずれる。
- 圧縮: セクション領域の角度半幅は `stepRad / 2`。隣接セクションがある側だけこの制限を適用し、
  外側に隣がなければ制限しない。加えて全プルトの角度上限80度(`STRING_TEMPLATE_MAX_ANGLE_RAD`)を
  併用するため、圧縮しても必ず指揮者より画面上側に収まる。制限を超える列だけ列全体を等比で
  圧縮し、比率が0.8未満なら`rowCompressed`警告を出す。

### コントラバスの配置方法

共通最前列円弧へは含めない(`onFrontArc = false`)。

- 奏者位置は`contrabass-stool`プリセット。通常椅子へ置き換えない。
- 列分配は通常弦と別関数`distributeDoubleBassPults()`。1列あたり最大3人を目安に
  `ceil(人数 / 3)`列(上限4列)とし、プルトを均等分配して余りを前列へ寄せる。
  3人→[2] / 4人→[1,1] / 6人→[2,1] / 8人→[2,1,1]。
- プルト内椅子間隔とプルト間隔はスツール実寸(900mm)から重ならない値へ広げる(1000mm / 1900mm)。
- アンカーは通常弦の最終列より`cbRearGapMm`(1000mm)だけ後方。左右は選択肢ごとに実座標へ反映する。
  図面右後方=弦全体の右端+セクション間隔+Cb半幅、図面左後方=左端-同、
  チェロ後方・ヴィオラ後方=対象セクションの先頭プルトの左右位置。既定は図面右後方。
- 後列は千鳥ではなく外側へ累積してずらすため、短い斜め列になる。

### 仕様上の決定

- 舞台奥は`STAGE_DEPTH_DIRECTION = { xMm: 0, yMm: -1 }`、図面右は`STAGE_RIGHT_DIRECTION = { xMm: 1, yMm: 0 }`の
  固定値。`stageFront.yMm`から方向を推測しない。`stageFront`は指揮者位置Yの初期値算出にのみ使う
  (`stageFrontY - 900`で舞台奥へ入れる)。
- 配置プリセットは`Vc中央・Va右前`(1st Vn→2nd Vn→Vc→Va)、`Va中央・Vc右前`(1st Vn→2nd Vn→Va→Vc)、
  `対向`(1st Vn→Va→Vc→2nd Vn)、カスタム順序。並び順は共通最前列円弧上のアンカー角へ直接反映される。
- v1.1との差異: v1.1の初期値(プルト間隔750mm・セクション間隔600mm、8型の人数8/6/6/4/3)は採用せず、
  現行の1100mm/300mm・8/6/4/4/2を維持した。今回の指示に初期値変更が含まれていないため。
  プルト間隔750mmは椅子中心間隔600mm+椅子幅450mm=1050mmより狭く、`pultPitchTooNarrow`警告の対象になる。
- 最前列距離1820mm・セクション間隔300mm(既定値)では、先頭プルト間隔に対して後列の必要幅が大きく、
  第1・第2ヴァイオリンの後列が等比圧縮されて警告が出る。12型では最前列距離3000mm・
  セクション間隔600mmで警告が解消し、最前列円弧が約97度・左右幅約12mになる。
- `SceneObject`・`schemaVersion`・`deserializeProject`・既存のプルト弧状配置は変更していない。

### 変更内容

- src/core/stringSectionTemplate.ts: 配置計算を作り替えた。追加: `stageFrame` / `stagePolarPointMm` /
  `stageLocalOf` / `computeStringFrontArc` / `distributeStringSectionPults` / `distributeDoubleBassPults` /
  `stringRowPlan` / `stringSectionGroups`。削除: `stageFrameFor` / `stringSectionRegions` /
  `STRING_SECTION_MAX_PULTS_PER_ROW` / `STRING_TEMPLATE_ARC_STRENGTH` /
  `STRING_TEMPLATE_INNER_SECTION_DEPTH_RATIO` / options の `stageFrontYMm`。
- src/App.tsx: セッション生成へ背景高さを渡し、`stringSectionTemplateOptionsFromSession`の引数を更新。警告算出へlayoutを渡す。
- src/components/StringSectionTemplateDialog.tsx: 概要へ最前列円弧の開き角、列構成(セクションごとの分配)を表示。
- src/core/arrangement.ts / src/state/appState.ts: 変更なし(`createPultUnitObjects`と`ADD_STRING_SECTION_TEMPLATE`を再利用)。
- src/core/stringSectionTemplate.test.ts / src/state/appState.stringTemplate.test.ts: 必須24項目へ合わせて書き替えた。

### 自動検証

- npm test: 42ファイル・**284テスト通過**(本機能は`stringSectionTemplate` 53、`appState.stringTemplate` 15)。既存テストの削除・skipなし。
- npm run typecheck: 通過。
- npm run build: 通過。チャンクサイズ警告は従来どおりPDF.js同梱によるもの。
- ブラウザ確認(Chromium、舞台前端Y=12900・指揮者(5000, 12000)):
  - 指揮者位置Yが舞台前端-900=12000で初期化され、全60オブジェクトのYが指揮者より小さい(画面上=舞台奥)。
  - 4セクションの先頭プルトの椅子がすべて半径3015mmの同一円弧上。図面左から
    1st Vn(-2687, -2362) → 2nd Vn(-1265, -698) → Vc(+698, +1265) → Va(+2362, +2687)。
  - 列構成表示が `1st Vn 1+2+3 / 2nd Vn 1+2+2 / Vc 1+2 / Va 1+1+2 / Cb 1+1`。
  - 全60オブジェクトの指揮者向きコサインが1.000000000。
  - Cbは半径8187mm・奥行き4378〜5730mmでcontrabassシンボル。通常弦より後方かつ図面右。
  - 3プリセットで先頭プルトのアンカー位置が実際に入れ替わる
    (Vc中央: Vc +839/Va +2254、Va中央: Va +839/Vc +2254、対向: 1st Vn -2254/2nd Vn +2254)。
  - プレビューのtransform 60件と確定後60件が完全一致。確定で60個生成・60個選択、
    1回のUndoで0個、1回のRedoで同一transformへ復元。プレビューは確定後に消える。
  - 最前列距離1820/セクション間隔300では`rowCompressed`警告、3000/600では警告なし。
  - プレビュー中はproject.objects・履歴・保存状態が不変。新規のconsole errorなし。

### 人間による確認事項

38. 実際のホール図面で12型を配置し、最前列距離3000mm・セクション間隔600mm程度の扇形が参考配置と一致するか確認する。既定値(1820/300)のままにするか、初期値自体を見直すか判断する。
39. 共通最前列円弧の全開き角上限120度と、全プルトの角度上限80度が実運用に合うか確認する。外側セクションの最後列が指揮者の真横近くまで回り込む場合はセクション間隔を詰める。
40. 前列1プルト固定の列分配(12型で1+2+3など)が、参考配置の各セクションの見え方と一致するか確認する。
41. 千鳥ずらし(半プルト)と外側ずらし(0.25プルト/列)の量が実配置で妥当か確認する。
42. コントラバスの斜め列(1900mmピッチ)と、通常弦最終列からの1000mmが実運用に合うか確認する。
43. `rowCompressed`警告が出る設定で、圧縮された列の間隔が実用に耐えるか目視確認する。
44. iPad Safariで、ドラッグを使わず「左へ」「右へ」ボタンのみでセクション順を変更し、配置確定まで完了できることを確認する。
45. 生成物を個別に移動・回転・削除・複製・グループ化でき、PNG/PDF出力へプレビュー表示が含まれないことを確認する。
46. 舞台前端を未設定のまま起動した場合、指揮者位置Yが背景高さの80%になる。この初期値が実運用に合うか確認する。

## 2026-07-26 弦楽器テンプレ配置を12型固定テンプレートへ変更

共通最前列円弧モデル(および過去のセクション別領域モデル・人数からの自動列分配)を廃止し、
参考配置画像から読み取った**20個の固定アンカー**だけで12型標準配置を生成する方式へ変更した。
今回の対象は12型のみで、人数編集・カスタム編成・奇数人数・他の型・自動再配置・舞台サイズ圧縮は実装していない。

### 従来の配置が不自然だった原因

1. プルト位置を「指揮者中心の角度」で作るため、列が円弧上で蛇行し直線的な列にならなかった。
2. セクション領域の角度半幅で後列を等比圧縮するため、既定値ではプルト間隔が実寸より詰まった。
3. プルト内椅子間隔600mmに対して隣接プルト間隔1100mmが近すぎた。プルトの席軸は指揮者方向へ直交するため、
   中央寄りのセクションでは席軸が列の進行方向とほぼ平行になり、隣接プルトの席が実質560mm程度まで接近して
   4人以上が一列に並んで見えた。
4. 1+2+3の自動列分配が三角形を作り、参考画像の「ほぼ一直線＋外端折り返し」にならなかった。

### 参考配置画像から採用した特徴

`docs/20210313075233.png` / `docs/crop_12_left.png` / `docs/crop_12_right.png` を参照した(アプリへは組み込んでいない)。

- 1st Vn: 左前方のほぼ水平な一直線。各プルトの2席は上下ペア(指揮者が右にあるため席軸が縦)。第6プルトだけ左外端から舞台奥へ折り返す。
- 2nd Vn: 1st Vnより奥の独立したまとまり。手前の短い列＋後列で千鳥。
- Va: 右前方の一直線。2席は上下ペアで譜面台は指揮者側(左)。
- Vc: Vaより奥で、手前2プルト＋その間の後列1プルト。
- Cb: 通常弦から離れた右後方の短い斜め列。
- 全体として位置は列で整列させ、向きだけを指揮者へ向ける。指揮者正面は空ける。

### 20アンカーの定義方法

`src/core/stringLayout12.ts` の `STRING_LAYOUT_12_ANCHORS` に、指揮者(指揮台中心)を原点とする相対mmで保持する。
`centerXMm`=図面右へ正、`centerYMm`=舞台奥へ正。人数からは一切計算しない。

| セクション | プルト | (右, 奥) mm | layoutRole |
|---|---|---|---|
| 1st Vn | 1〜5 | (-1500,1600) (-2700,1640) (-3900,1690) (-5100,1730) (-6300,1750) | front-line |
| 1st Vn | 6 | (-6000,3400) | outer-return |
| 2nd Vn | 1〜3 | (-1400,3250) (-3000,3300) (-4600,3350) | inner-arc |
| 2nd Vn | 4〜5 | (-2200,4500) (-3800,4550) | rear-line |
| Va | 1〜4 | (1500,1600) (2700,1640) (3900,1690) (5100,1730) | front-line |
| Vc | 1〜2 | (1400,3250) (3000,3300) | inner-arc |
| Vc | 3 | (2200,4500) | rear-line |
| Cb | 1〜2 | (5800,3800) (7200,4900) | bass-diagonal |

### プルト内部の生成

`createStringPultUnitObjects`(純粋関数)がアンカー1個からSceneObject 3個を作る。
既存の共通処理`createPultUnitObjects`(arrangement.ts)へ委ね、椅子・譜面台の生成経路を単体配置と共通化している。

- 椅子2脚: プルト中心から指揮者方向へ直交する軸上へ±(椅子間隔/2)で対称配置。
- 譜面台1台: 2席の中間から指揮者側へ450mm。半径方向に置くので必ず椅子より指揮者側になる。
- 回転角: 各オブジェクト位置から指揮者位置へ向かう角度(`rotationForPoint`)。プルト単位の固定値ではない。
- 位置と向きを分離しているため、前列を蛇行させずに向きだけで指揮者方向を表現できる。

### 採用した間隔

- プルト内椅子中心間隔: 600mm。Cbのみ`contrabass-stool`実寸(900mm幅)に合わせて1100mm。
- 隣接プルト中心間隔: 1st Vn / Va は1200mm、2nd Vn / Vc は1600mm。
  中央寄りの2セクションは席軸が列方向とほぼ平行になるため、指示の1150〜1250mmでは隣接プルトの席が
  600mm(=プルト内間隔)まで接近して「2人1組」に見えない。指示の「必要なら広げてよい」に従って広げた。
- 前後列中心間隔: 1200mm(1st Vnの折り返しのみ1650mm)。
- セクション間余白: 1200mm以上。
- 実測: 異なるプルトの席どうしの最小中心間距離は861mm(椅子縁で411mmの空き)。プルト内は600mm(縁で150mm)。

### 舞台奥方向の判定方法

`STAGE_DEPTH_DIRECTION = { xMm: 0, yMm: -1 }`(画面上)、`STAGE_RIGHT_DIRECTION = { xMm: 1, yMm: 0 }`の固定値。
`stageFront.yMm`と指揮者位置Yの大小関係からは推測しない。根拠は、椅子・譜面台シンボルの基準方向が
回転0で-Y(`firstPersonPoseForObject`と同じ規約)であることと、図面上で客席が画面下になる本アプリの前提。
`stageFront`は指揮者位置Yの初期値算出(`stageFrontY - 900`)にだけ使う。
全体補正の入力範囲(倍率0.8〜1.5、前後補正-500〜5000mm)は、最小アンカー奥行き1600mmに対して
全オブジェクトが必ず指揮者より舞台奥側に残るよう決めている。

### 変更内容

- src/core/stringLayout12.ts(新規): 固定アンカー20個、`StringPultAnchor`、`StringLayout12Options`、
  `stringLayout12Placements` / `createStringPultUnitObjects` / `createStringLayout12Objects` /
  `validateStringLayout12Options` / `stageLocalPointMm` / `stageLocalOf` / `facingRotationDeg` / 各定数。
- src/core/stringSectionTemplate.ts: 配置計算を持たないUI層へ縮小。確定前セッション、配置オプション組み立て、
  起動条件、警告(範囲外・オブジェクト数)、編成型プリセット表示データのみ。旧アルゴリズム
  (共通最前列円弧・後方扇形・列分配・千鳥・セクション順・Cb位置4種)は削除した。
- src/state/appState.ts: `ADD_STRING_SECTION_TEMPLATE`の引数型を`StringLayout12Options`へ変更し、
  `createStringLayout12Objects`を呼ぶ。1回の`commitProject`で全60個を追加し全生成物を選択する構造は不変。
- src/App.tsx: プレビューを`stringLayout12Placements` / `createStringLayout12Objects`で作り直す。
- src/components/StringSectionTemplateDialog.tsx: 選択肢を「12型標準配置」だけにし、8/10/14/16型は
  選択不能ボタンとして残す。人数入力・セクション順・コントラバス位置・4つの間隔入力を削除し、
  左右位置補正／前後位置補正／全体間隔倍率の3項目にした。
- src/components/PropertyPanel.tsx: 起動ボタンの説明文のみ更新。
- src/core/arrangement.ts: 無変更。既存のプルト弧状配置・椅子多列円弧配置の座標結果と外部仕様は変えていない。
- SceneObject・schemaVersion・deserializeProject・phase4.cssは無変更。外部ライブラリの追加なし。

### 追加・変更したテスト

- src/core/stringLayout12.test.ts(新規・29テスト): 20プルト、椅子36＋スツール4＋譜面台20、プルト単位の構成、
  プルト内が他プルトより近いこと、隣接プルトの余白800mm以上、セクション間余白600mm以上、採用間隔の実測、
  1st Vn 6プルト／第1〜5の直線性(三角形でないこと)／第6の折り返し、2nd Vnの3＋2と千鳥、Vaの直線4プルトと
  指揮者正面の空き、Vcの2＋1、Cbの右後方斜め列と専用スツール、補正の全範囲で全オブジェクトが舞台奥側、
  全オブジェクトの指揮者向きコサイン、譜面台が椅子より指揮者側、全体補正の性質、入力検証、ラベル。
- src/core/stringSectionTemplate.test.ts(全面書き替え・9テスト): 編成型プリセット、起動条件、セッション初期値、
  オプション組み立て、プレビューと確定が同一オプション・同一関数を通ること、警告と背景範囲。
- src/state/appState.stringTemplate.test.ts(全面書き替え・13テスト): 1 Actionで60生成・全選択・履歴1件、
  プレビューと確定の座標／角度一致、プレビューが履歴・保存状態・選択を変えないこと、1回のUndo/Redo、
  確定後の個別編集、セクションの左右／前後関係、舞台前端の値によらず画面上側へ生成、レイヤー制約、
  無効な補正値の拒否、ID/zIndexの重複なし、保存・復元の往復、既存のADD_PULT_ARC / ADD_CHAIR_ARC_ROWSの回帰。

### 自動検証

- npm test: 43ファイル・**267テスト通過**(本機能は`stringLayout12` 29、`stringSectionTemplate` 9、`appState.stringTemplate` 13)。既存テストの削除・skipなし。
- npm run typecheck: 通過。
- npm run build: 通過。チャンクサイズ警告は従来どおりPDF.js同梱によるもの。
- ブラウザ確認(Chromium 1280x720、校正後・指揮者(5000, 8000)):
  - パネルの選択肢が「12型標準配置」のみ有効で、8/10/14/16型は`disabled=true`。
  - プレビュー60個を描画し、`pointer-events: none`で選択・ポインター判定の対象外。
  - 「配置する」後、プレビュー60件のtransform文字列が確定後のオブジェクトと完全一致(60/60)。
  - 確定後にプレビュー群が消え、パネルが閉じる。新規のconsole errorなし。

### 人間による確認事項

47. 実際のホール図面で12型を配置し、参考配置画像との差(とくに1st Vn第6プルトの折り返し位置と、2nd Vn・Vcのプルト間隔1600mm)が実運用に合うか目視確認する。
48. 全体の左右幅が約13.9m、前後が指揮台から約5.4mになる。実際の舞台に収まるか、全体間隔倍率で調整して確認する。
49. Cb用スツール(900×1200mm)2脚＋譜面台のプルトが、実際のコントラバス奏者の占有域として妥当か確認する。
50. 生成後に椅子・譜面台を個別に移動・回転・削除・複製・グループ化でき、PNG/PDF出力にプレビュー表示が含まれないことを確認する。
51. iPad Safari実機で、パネルの補正3項目の入力と「配置する」まで完了できることを確認する。
52. 舞台奥を画面上(Y負方向)としているため、図面が上下逆のプロジェクトでは配置が客席側に見える。その運用でよいか確認する。

## 2026-07-31 操作性改修(インスペクター再配置・スナップUI復活・向き操作・配置時の自動回転)

作図1件あたりの操作回数を減らすための改修。配置アルゴリズム・mm正本・schemaVersion・出力は変更していない。

### 発見した問題と対応

| ID | 優先度 | 問題 | 対応 |
|---|---:|---|---|
| UX-OPS-001 | P1 | インスペクターの「プロパティ」が固定順の最後にあり、1オブジェクト選択するたびに約1,100pxスクロールしないと座標・角度を編集できなかった(1280x720実測: 表示高さ597pxに対し内容2399px、`角度 (度)`が上から1728px)。 | 修正済み。プロパティを最上段へ移動し、一括配置・スナップ・レイヤー・背景・編集ガイド・壁を`<details class="inspector-section">`で折りたたんだ。表示スタイルも既定で閉じた。実測で`X (mm)`250px、`角度 (度)`540pxへ短縮。 |
| UX-OPS-002 | P1 | `SnapPanel.tsx`がどこからもimportされておらず、`SET_SNAP_SETTINGS`を発行するUIがアプリ内に存在しなかった。グリッド・他オブジェクト・舞台中央線・編集ガイドのスナップを利用者が有効にできなかった。 | 修正済み。インスペクターへ「スナップ」セクションとして復帰。既定値は舞台中央線と編集ガイドをON、グリッド(910mm)と他オブジェクト中心は椅子の実ピッチと衝突するためOFFのまま。 |
| UX-OPS-003 | P2 | グリッドスナップを有効にしてもキャンバスにグリッドが描かれず、スナップ先が見えなかった。 | 修正済み。`renderSnapGrid()`で`snapSettings.grid`が有効なときだけ実寸グリッドを描画。mm間隔が画面上2px未満の倍率では描かず、線数は400本で打ち切る。 |
| UX-OPS-004 | P1 | 「指揮台へ向ける」「指定点へ向ける」「角度を統一」「±5度」が2個以上選択時のみで、1脚だけ向きを変えるには数値入力か回転ハンドルのドラッグしかなかった。 | 修正済み。向き操作を`renderOrientationTools()`へ分離し、1個選択でも表示する。±15度を追加。基準点指定と左右対称複製は`<details>`へ格納した。 |
| UX-OPS-005 | P2 | 「指揮台へ向ける」の基準が選択範囲内の指揮台に限られ、椅子だけを選ぶと使えなかった。 | 修正済み。選択に指揮台が含まれない場合は、選択範囲の重心から最も近い図面上の指揮台を基準にする。 |
| UX-OPS-006 | P1 | 配置物の初期角度が常に0度で、指揮者向きの編成では1個置くごとに回転操作が付いていた。 | 修正済み。ライブラリの配置操作へ「指揮台へ向ける」チェックを追加(既定OFF、指揮台が0個なら無効)。配置位置から最も近い指揮台へ`rotationDegTowardPoint`で向ける。 |
| UX-OPS-007 | P2 | `deserializeProject`が`snapSettings.guides` / `guideThresholdMm`を旧JSONへ補完せず、既定値へ寄らなかった。 | 修正済み。他のスナップ項目と同じく`createEmptyProject`の値へフォールバックする。 |

### 変更内容

- `src/state/appState.ts`: `placementFacePodium`と`SET_PLACEMENT_FACE_PODIUM`を追加(Projectには保存しない表示側の設定)。`createGroupPresetObjects`へ`rotationDeg`を追加し、パーツのオフセットをセット中心まわりに回して剛体回転させる。`ADD_GROUP_PRESET` / `ADD_CHAIR_MUSIC_STAND_SET`へ任意の`rotationDeg`を追加(未指定時は従来どおり0度)。
- `src/components/CanvasStage.tsx`: `placementRotationDegAt()`を追加し、単体プリセット・椅子譜面台セット・ティンパニ/トムセットの配置角度へ反映。`renderSnapGrid()`を追加。
- `src/components/PropertyPanel.tsx`: セクション順を「プロパティ → 一括配置 → スナップ → レイヤー → 必要物一覧 → 背景 → 編集ガイド → 壁」へ変更し折りたたみ化。`renderOrientationTools()`(1個以上)と`renderMultipleProperties()`(2個以上)へ分離。指揮台の最寄り判定`nearestPodium` / `selectionCenterMm`を追加。
- `src/components/LibraryPanel.tsx`: 「指揮台へ向ける」チェックと説明を配置操作へ追加。
- `src/components/SnapPanel.tsx`: インスペクターへ組み込み。見出しを`<summary>`へ移し、整列ガイドが常時働くことを明記。
- `src/components/BackgroundPanel.tsx` / `LayerPanel.tsx` / `GuidePanel.tsx` / `PropertyPanel.tsx`の`WallPanel`: 見出し`<h2>`を外側の`<summary>`へ移動。
- `src/core/project.ts`: `createEmptyProject`のスナップ既定値を`stageCenter: true` / `guides: true`へ変更。`deserializeProject`のguides系フォールバックを修正。
- `src/core/snap.ts`: `DEFAULT_SNAP_SETTINGS.stageCenter`をtrueへ。
- `src/ui-refresh.css` / `src/phase4.css`: `.inspector-section`、`.orientation-advanced`、`.snap-grid`のスタイルを追加。
- 配置アルゴリズム(`arrangement.ts` / `stringLayout12.ts` / `stringSectionTemplate.ts`)、`SceneObject`、`schemaVersion`、出力処理は無変更。外部ライブラリの追加なし。

### 追加・変更したテスト

- `src/state/appState.placementFacing.test.ts`(新規・6テスト): `placementFacePodium`の既定値と切り替え、Project/履歴/保存状態を変えないこと、`rotationDegTowardPoint`の向き規約(指揮台の真下=0度)、`ADD_GROUP_PRESET`のrotationDeg未指定時の後方互換、90度指定時の剛体回転と寸法非入替(AC-006)、椅子譜面台セットの向き。
- `src/core/project.phase4.test.ts`: スナップ既定値の期待値を新しい既定へ更新。往復テストのfixtureへ`guides` / `guideThresholdMm`を追加し、往復が完全一致することを確認。

### 自動検証

- npm test: 66ファイル・**437テスト通過**。既存テストの削除・skipなし。
- npm run typecheck: 通過。
- npm run build: 通過。チャンクサイズ警告は従来どおりPDF.js同梱によるもの。
- ブラウザ確認(Chromium 1280x720): インスペクターの内容高さが選択なしで1038px(従来1335px)、1個選択で2029px(従来2399px)。`X (mm)`250px・`角度 (度)`540px・「指揮台へ向ける」635px・「±15度」674px。指揮台を1つ置くと配置チェックが有効化。「指揮台へ向ける」ONで指揮台(7500,2750)の下・左・右・上に置いた椅子がそれぞれ0度・90.36度・269.64度・180度で生成。グリッドONで910mm間隔の線25本を描画。7個選択で向き操作1組＋一括操作1組が重複なく表示。新規読み込み時のconsole error 0件。

### 人間による確認事項

53. 実際のホール図面で、折りたたんだインスペクターの初期開閉状態(プロパティ・一括配置・レイヤーが開、他が閉)が実運用に合うか確認する。開閉状態は保存していないため、毎回既定へ戻る。
54. スナップ既定値(舞台中央線ON・編集ガイドON・グリッドOFF・他オブジェクト中心OFF)が実作業に合うか確認する。既存の保存済みプロジェクトは保存時の値が優先されるため、新規プロジェクトでのみ既定が効く。
55. グリッド910mm・閾値60mmでの椅子配置が実用に耐えるか、間隔を600mmなどへ変えて確認する。
56. 「配置時に指揮台へ向ける」を使い、指揮台が複数ある図面で最寄り判定が意図どおりか確認する。
57. iPad Safari実機で、折りたたみセクションの開閉と「指揮台へ向ける」チェックがタッチで操作できることを確認する。
