# MVP・Phase 4・UX監査受入確認ハンドオフ

更新日: 2026-07-18
対象: docs/要件定義書_v1.0.md AC-001〜AC-016、AC-101
Phase 4実装コミット: 本Phaseの最終コミット

## AC確認状況

| ID | 状態 | 検証内容・残る確認 |
|---|---|---|
| AC-001 | テストで検証済み | 10,000mm校正後の再測定をphase1Acceptance.test.tsで確認。 |
| AC-002 | テストで検証済み | 910mm校正と450mm椅子の実寸比率を確認。 |
| AC-003 | テストで検証済み | ズーム・パンがmm正本を変更しないことをtransform.test.tsで確認。 |
| AC-004 | テストで検証済み | PDFページ、背景90度回転、切り抜きの保存・復元を確認。 |
| AC-005 | テストで検証済み | 椅子の500×550mm編集と外接矩形を確認。 |
| AC-006 | テストで検証済み | 回転後もwidthMm/depthMmを入れ替えないことを確認。 |
| AC-007 | テストで検証済み | 水平・垂直の整列と等間隔配置を確認。 |
| AC-008 | テストで検証済み | 配置・移動・回転・削除のUndo/Redoとドラッグ履歴を確認。 |
| AC-009 | テストで検証済み | 背景、校正、オブジェクト、heightMmのJSON保存復元と旧形式移行を確認。 |
| AC-010 | 実装済みだが人間の確認待ち | mm正本からPNGを再描画し編集UIを除外。ブラウザで背景・配置物の相対位置を目視確認する。 |
| AC-011 | 実装済みだが人間の確認待ち | pdf-libでA4/A3・縦横のPDFを生成。PDFビューアで1ページと情報欄を確認する。 |
| AC-012 | 実装済みだが人間の確認待ち | 1:50/1:100のPDF寸法計算をテスト済み。100%印刷し、1820mm基準線を実測する。 |
| AC-013 | テストで検証済み | 未校正時の配置・出力禁止、警告、ライブラリ無効化を確認。 |
| AC-014 | テストで検証済み | 非対応形式、破損PDF、破損JSONを安全に拒否することを確認。 |
| AC-015 | 実装済みだが人間の確認待ち | Pointer Events、ピンチ／パン、回転、レスポンシブUIを実装。実機iPad Safariで一連の操作を確認する。 |
| AC-016 | テストで検証済み | 山台のheightMm=300、段高ラベル、保存復元を確認。 |
| AC-101 | テストで検証済み | appState.phase4.test.tsで14プルト=椅子28脚＋譜面台14台を1 Actionで生成し、arrangement.test.tsで弧状座標と15度単位の向きを確認。 |

## UX監査の実施内容

- コード、reducer、既存テスト、保存／出力導線を確認。
- 開発サーバーをブラウザで操作し、背景読込済み・校正済み・オブジェクト配置後の状態を確認。
- 768×1024px（iPad縦持ち相当）でDOM、レイアウト寸法、コンソール警告／エラーを確認。
- 校正、単発配置、モード切替、矢印キー移動、出力ダイアログを実操作で確認。
- ブラウザ監査時のコンソール error/warn は0件。

## 発見事項と対応状況

| ID | 優先度 | 発見した問題 | 対応状況 |
|---|---|---|---|
| UX-AUDIT-001 | P1 | JSON「開く」が未保存変更を確認せず、現在の編集を破棄する可能性があった。 | 修正済み。未保存時は読み込み前に確認し、キャンセル時はファイル選択を開始しない。 |
| UX-AUDIT-002 | P1 | 通常の選択モードはツールバーの色だけで、現在モードの意味が常時伝わりにくかった。 | 修正済み。現在モード、配置待機、連続配置中をテキストとARIA状態で表示。 |
| UX-AUDIT-003 | P1 | 選択物を細かく動かすためのキーボード操作がなく、タッチ／マウスだけでは位置調整に手間がかかった。 | 修正済み。選択中の矢印キーを10mm、Shift+矢印を100mmのActionへ集約し、キャンバスをキーボードフォーカス可能にした。 |
| UX-AUDIT-004 | P1 | 768px幅では左右パネルとキャンバスが横に詰まり、タップ対象と作業領域が狭かった。 | 修正済み。900px以下はライブラリ→キャンバス→プロパティの縦配置に切り替え、主要ボタンと入力欄を44px以上にした。 |
| UX-AUDIT-005 | P2 | 校正距離の910/1820mmが数値だけで、半間・1間との対応を画面上で再確認しにくかった。 | 修正済み。ボタンを「半間・3尺（910mm）」「1間（1820mm）」と表示し、任意距離の説明も補強。 |
| UX-AUDIT-006 | P2 | プリセット寸法に単位がなく、ピアノやティンパニの値が正確な機種実寸なのか標準値なのか判断しにくかった。 | 実装済みだが人間の確認待ち。mm表示と「標準初期値・機種差あり」の説明を追加。実物仕様に合わせた最終値はホール備品／機種ごとに確認する。 |
| UX-AUDIT-007 | P2 | PDFの1:50、1:100、フィットの違いと印刷時の注意が1文だけで分かりにくかった。 | 修正済み。各縮尺の意味、1820mmの紙面寸法、フィット時の実寸非保証、倍率100%印刷を表示。 |
| UX-AUDIT-008 | P2 | プロパティの数値欄を空にしてから置き換える操作や、細かなタッチ領域には改善余地が残る。 | 将来対応。既存のAction・mm正本を維持したまま、入力コミット方式と実機操作を別途改善する。 |

## UX受入条件

| ID | 状態 | 根拠・残る確認 |
|---|---|---|
| UX-001 | テストで検証済み | ライブラリ、未校正バナー、校正ダイアログに背景読込→校正→配置の説明を追加・確認。 |
| UX-002 | テストで検証済み | 既存の単発配置reducerテストとブラウザ実操作で、配置直後に選択状態になることを確認。 |
| UX-003 | テストで検証済み | 連続配置Action、配置待機文言、連続配置バッジを確認。 |
| UX-004 | 実装済みだが人間の確認待ち | Shift解除時の待機解除ロジックは実装済み。実機キーボードで押下・解除の一連の操作を確認する。 |
| UX-005 | テストで検証済み | calibrationDistanceLabelの単体テストと校正ダイアログのブラウザ表示を確認。 |
| UX-006 | テストで検証済み | モード表示名の単体テスト、aria-pressed、ブラウザでの範囲選択表示を確認。 |
| UX-007 | 実装済みだが人間の確認待ち | 未校正・未保存・ロック・連続配置・現在モードの表示を実装。画面全体の視認性は実機／実画面で確認する。 |
| UX-008 | 実装済みだが人間の確認待ち | Pointer Events、フォーカス可能キャンバス、900px以下のタッチサイズを実装。iPad Safari実機で読込・ピンチ・移動・回転を確認する。 |
| UX-009 | テストで検証済み | Undo/Redo既存テストと、矢印キー相当のNUDGE_SELECTED reducerテストを確認。 |
| UX-010 | テストで検証済み | npm test、npm run typecheck、npm run buildが成功。 |

## 今回の変更ファイルと動作変更

- `src/state/appState.ts`: モード表示名と `NUDGE_SELECTED` Actionを追加。既存のmm座標・end座標・ロック判定・Undo履歴を通る。
- `src/App.tsx`、`src/components/CanvasStage.tsx`: 選択中の矢印キー移動、キャンバスフォーカス、入力欄・ボタンへの誤発火防止を追加。
- `src/components/Toolbar.tsx`: 現在モード、配置待機、連続配置の表示、モードARIA状態、未保存のJSON読込確認を追加。
- `src/components/LibraryPanel.tsx`、`src/core/presets.ts`、`src/components/CalibrationDialog.tsx`: 配置手順・標準初期値・mm単位・校正距離の意味を明示。
- `src/components/ExportDialog.tsx`: 1:50／1:100／フィットと100%印刷の説明を追加。
- `src/phase3.css`、`src/phase4.css`: 900px以下の縦レイアウトと44pxタッチターゲットを追加。
- `src/core/calibration.test.ts`、`src/state/appState.test.ts`、`src/state/appState.phase2.test.ts`: 校正ラベル、モード表示、mmナッジの回帰テストを追加。
- `README.md`、`HANDOFF.md`: 実装範囲、UX監査結果、受入状況、手動確認事項を更新。

## Phase 4の実装内容

- レイヤー: 背景、オブジェクト、注釈の表示／非表示、ロック、アクティブレイヤー。
- 注釈: テキスト、線、矢印、矩形、円、寸法線。終点もmmで保存し、選択・PNG/PDF出力に対応。
- スナップ: グリッド、他オブジェクト中心、舞台中心線。間隔・閾値はプロジェクトへ保存。
- 行列配置: 行数、列数、左右／上下間隔を指定し、元オブジェクトを基準に複製。
- プルト弧状配置: 中心、半径、開始／終了角度、プルト数、プルト間隔を指定。生成後は個別選択・編集可能。
- 山台関連付け: プロパティの選択欄から onRiserId を手動指定。
- 追加プリセット: 演台、アップライトピアノ、チェレスタ、バスドラム、ヴィブラフォン、シロフォン／グロッケン、チャイム、ドラムセット、ティンパニ23/32インチ、アンプ／スピーカー。

## 自動検証結果

- npm test: 19テストファイル、74テスト通過。
- npm run typecheck: 通過。
- npm run build: 通過。
- ビルド時のチャンクサイズ警告はPDF.jsを同梱しているためで、ビルド失敗ではない。
- PDF.jsのNodeテスト時にlegacy build利用の警告が出るが、テスト失敗ではない。
- 開発サーバー: http://127.0.0.1:5173/

## 人間による確認事項

1. 校正済みプロジェクトでPNGを出力し、背景・配置物・注釈の位置と編集UI非表示を確認する。
2. A3横・1:100 PDFを出力し、PDFビューアで1ページ、注釈、出力情報欄を確認する。
3. 同PDFをプリンタ倍率100%で印刷し、1820mm基準線が18.2mm±1mmか実測する。
4. 実機iPad Safariで、背景読込→校正→配置→移動→回転→複製→注釈→PDFの順に確認する。
5. レイヤーの表示／ロック、14プルト弧状配置、行列配置、山台関連付けをブラウザで確認する。
