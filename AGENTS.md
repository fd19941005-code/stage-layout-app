# AGENTS.md — 舞台図面作成アプリ

このファイルは、Codexがこのリポジトリで作業する際に常に守る恒久ルールだけを定める。現在の実装状況・既知の問題・作業履歴は、ここへ追記せず `docs/` の案内先を参照する。

## 作業開始時に読む文書

1. `README.md` — プロジェクト入口と文書の読み方
2. `docs/current-state.md` — 調査日時点の実装状況と次の候補
3. 作業内容に応じた文書
   - 構造・データフロー: `docs/architecture.md`
   - 用語・ドメイン制約: `docs/domain.md`
   - テスト・受入評価: `docs/testing.md`
   - 既知の問題・制約: `docs/known-issues.md`
   - 設計理由・過去の失敗: `docs/decisions/`
   - FR/NFR/ACの正式な要求: `docs/要件定義書_v1.0.md`

文書の役割とSource of Truthの対応は `docs/README.md` にある。日付付き監査・引き継ぎ・仕様プロンプトは履歴または参考資料であり、現在状態の根拠として単独で使わない。

## project-memory の実行ルール

重要な実装・修正・設計変更・調査を完了する前に、今回の作業によって将来のCodexセッションにも必要となる恒久的なプロジェクト知識が得られた可能性がある場合は `$project-memory` を実行する。明示的に指定された場合は必ず実行する。

特に、重要な機能追加・仕様変更、アーキテクチャ・責務分担・データフローの変更、バグの根本原因や重要な設計判断の確定、採用しない方法や重要な失敗アプローチの判明、重要な制約・前提・不変条件の判明または変更、テスト・評価方法・受入条件の重要な変更、既知の問題・技術的負債・回避策の発見、現在の実装状況や主要なボトルネックの大きな変化を対象とする。

軽微な修正、typoや表面的な変更、一時的なデバッグ、未確認の仮説、コードから容易に復元できる通常の実装詳細、将来の開発に影響しない情報は原則として対象外とする。実行後に保存すべき恒久知識がないと判断した場合は、文書を変更しない。

AGENTS.mdは簡潔に保ち、詳細な知識は既存の適切なSource of Truthへ保存する。新しい文書を作る前に既存の文書体系を確認し、同じ情報を重複して記録せず、既存の構成を優先する。`$project-memory` Skillの詳細手順はこのファイルへコピーしない。

## 恒久的な不変条件

1. **mmが正本**: 配置物の位置・寸法・角度は `xMm/yMm/widthMm/depthMm/heightMm/rotationDeg` を正本とする。Canvas/SVGのpx値や描画ライブラリ固有オブジェクトをプロジェクトJSONへ保存しない。
2. **回転は描画変換**: 回転しても `widthMm` と `depthMm` を入れ替えない。
3. **座標変換は集約**: px⇔mmや表示変換は `src/core/transform.ts` に純粋関数として集約し、UIコンポーネントへ重複実装しない。変更時は単体テストを追加する。
4. **保存形式の互換性を守る**: スキーマ変更時は `src/types/project.ts` の `SCHEMA_VERSION` を上げ、`src/core/project.ts` のデシリアライズ／マイグレーションを追加する。既存JSONを読めなくする変更は禁止する。
5. **未知フィールドを保持方針に従って無視する**: `deserializeProject` の前方互換方針を壊さない。
6. **表示状態と実寸状態を分離する**: ズーム・パン・表示用の変換は実寸データを変更しない。
7. **編集はAction経由**: プロジェクトを変更する操作は `src/state/appState.ts` のreducer Actionとして実装し、UIから直接状態を書き換えない。
8. **入力はPointer Events APIで統一する**: マウス・タッチ・ペンの分岐を増やさず、既存のポインター処理を拡張する。
9. **外部送信・外部CDNを追加しない**: プロジェクトデータを外部サーバーへ送信せず、外部CDN依存も追加しない。デプロイ保護や公開設定はアプリのデータフローとは別に扱う。
10. **UI文字列は日本語**: 既存の用語・操作表現と整合させる。

## 変更範囲と進め方

- 依頼が調査・監査・文書整備のみなら、アプリ本体（`src/`）・CSS・設定・依存関係・生成物を変更しない。
- 作業開始時に対象ファイル、データ型の変更有無、テスト項目を確認する。1回の作業で複数の大きなPhaseを混ぜない。
- 既存の未コミット変更はユーザーの作業として扱い、内容を上書き・巻き戻し・整形しない。コミット、push、公開、外部サービス変更は依頼がある場合だけ行う。
- 新しい依存関係が必要な場合は、必要理由と代替案を記録してから追加する。
- 要件変更と実装状況の記録を混同しない。正式な要求は要件定義書、現状は `docs/current-state.md` を更新する。

## 最低限の検証

実装変更を完了とする前に、リポジトリルートで次を実行する。

```bash
npm test
npm run typecheck
npm run build
```

検証結果のSource of Truth、ならびに自動検証とブラウザ・iPad・印刷／PDF出力・公開確認の境界は `docs/testing.md` に記録する。ビルド成功だけで実機・視覚・出力受入を完了扱いにしない。
