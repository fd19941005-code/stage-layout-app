# AGENTS.md — 実装エージェント向け指示

舞台配置図作成アプリ(ホール舞台平面図への実寸オブジェクト配置・図面出力システム)。

## 必読ドキュメント

1. `docs/要件定義書_v1.0.md` — 確定要件。FR/NFR/AC番号はすべてこの文書を指す
2. `README.md` — 現在の実装範囲・ディレクトリ構成・ロードマップ

## 絶対に守る不変条件

これらに違反する変更は、他のすべての指示より優先して拒否すること。

1. **mmが正本**: 配置物の位置・寸法・角度は `xMm/yMm/widthMm/depthMm/heightMm/rotationDeg` のみを正本とする。Canvas/SVG上のpx値・描画ライブラリ固有オブジェクトをプロジェクトJSONへ保存しない(要件12.1)
2. **回転は描画変換**: 回転してもwidthMm/depthMmを入れ替えない(要件6.2、AC-006)
3. **座標変換はcore/transform.tsに集約**: px⇔mm変換ロジックをUIコンポーネントへ書かない。純粋関数として追加し、必ず単体テストを付ける(要件11.4)
4. **保存形式の破壊的変更禁止**: スキーマ変更時はschemaVersionを上げ、`core/project.ts` のdeserializeProjectにマイグレーションを追加する。既存のプロジェクトJSONが読めなくなる変更は不可(要件12.1)
5. **未知フィールドは無視して復元**: deserializeProjectの前方互換方針を維持する(要件6.3)
6. **ズームと寸法変更の分離**: 画面ズーム・パンは表示のみ。実寸データへ影響させない(AC-003)
7. **編集操作はAction経由**: すべての編集は `state/appState.ts` のreducer Actionとして実装する(将来のUndo/Redoコマンド履歴の前提)
8. **ポインター入力はPointer Events APIで統一**: マウス/タッチの分岐を最小化する(要件12.1)
9. **外部サーバーへデータを送信しない**(NFR-008)。外部CDN依存も追加しない(NFR-015)

## 完了条件(全タスク共通)

```bash
npm test           # 全テスト通過
npm run typecheck  # TypeScript strict エラーなし
npm run build      # ビルド成功
```

- 新機能には対応するテストを追加する。特に要件11.4「自動テスト必須領域」に該当するロジックはテストなしで完了としない
- 受入条件(AC-xxx)に対応する実装は、可能な限りACをテストコードへ変換する(要件13.1)

## 作業の進め方

- Phaseごとに小さな単位で進める。一度に複数Phaseへ手を出さない(要件13.1)
- Phase開始時に、変更対象ファイル・データ型の変更・テスト項目を先に列挙してから実装する
- 新しいライブラリを追加する場合、理由と代替案をPRまたはコミットメッセージに記録する。推奨: PDF入力=PDF.js、PDF出力=pdf-lib、3D=Three.js(遅延読み込み)(要件第12章)
- UI文字列は日本語(NFR-014)

## 現在の状態(2026-07-18時点)

Phase 0完了＋Phase 1〜2の一部先行実装:
- 実装済み: 型定義(heightMm/walls予約含む)、px⇔mm変換、2点校正+距離プリセット、画像背景読込、プリセット配置、単一選択・移動・複製・削除・ロック、数値編集、距離測定、localStorage自動保存、JSON入出力
- 次の候補: Phase 1の残り(PDF読込、背景回転・切り抜きUI、校正確認FR-024) → Phase 2(Undo/Redo、複数選択、整列・等間隔、回転ハンドル、iPadタッチ操作系)
- 詳細なロードマップと引き継ぎメモはREADME.md参照
